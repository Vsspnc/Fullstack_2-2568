function areaTriangle(base, height) {
    return 0.5 * base * height;
}

function areaCircle(radius) {
    return Math.PI * radius * radius;
}

function areaRectangle(length, width) {
    return length * width;
}

export { areaTriangle, areaCircle, areaRectangle };

